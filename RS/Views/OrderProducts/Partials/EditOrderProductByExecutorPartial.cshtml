@model RS.ViewModels.OrderProducts.OrderProductExecutorEditViewModel

<form id="editOrderProductByExecutorForm" method="post" asp-action="EditOrderProductByExecutor" asp-controller="OrderProducts">
    <h6>Редактировать товар в заказе (исполнитель)</h6>
    <input type="hidden" asp-for="Id" />

    <div class="form-group">
        <label for="Status">Статус</label>
        <select asp-for="Status" class="form-control" asp-items="@(new SelectList(RS.ViewModels.OrderProducts.OrderProductExecutorEditViewModel.AvailableStatuses))"></select>
        <span asp-validation-for="Status" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label for="AdditionalInfo">Дополнительная информация</label>
        <textarea asp-for="AdditionalInfo" class="form-control"></textarea>
        <span asp-validation-for="AdditionalInfo" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Сохранить изменения</button>
</form>
